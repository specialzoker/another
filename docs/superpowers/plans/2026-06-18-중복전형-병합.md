# 중복 전형 병합 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 표시상 동일한(같은 시트·대학·전형유형·전형명, 학과는 모집단위까지) 전형 행들을 빌드 단계에서 하나로 완전 병합해 검색 목록을 정리한다.

**Architecture:** `build/parse.py`에 순수 함수 `merge_records`를 추가하고, `build/build.py`가 parse 직후 이를 호출해 병합한 뒤 매칭·출력한다. 프런트엔드는 레코드 구조가 동일하므로 수정 없음.

**Tech Stack:** Python 3.12 + pytest. 프런트 변경 없음. 빌드 후 `web/data/*.js` 갱신 → git 커밋 → GitHub Pages 자동 재배포.

---

## File Structure
- `build/parse.py` — `merge_records(records)` + 보조 `_base_id`, `_weighted_avg` 추가 (기존 함수 미수정)
- `build/build.py` — `merge_records` import + parse 직후 1줄 호출
- `build/tests/test_merge.py` — 병합 단위 테스트 (신규)

병합 키 = `base-id`(레코드 id에서 끝 `#숫자` 제거). 기존 `#n` 접미 그룹과 정확히 일치.

---

## Task 1: merge_records 병합 함수

**Files:**
- Modify: `build/parse.py` (APPEND; 기존 함수 미수정)
- Test: `build/tests/test_merge.py`

- [ ] **Step 1: 실패하는 테스트 작성** — `build/tests/test_merge.py`:

```python
from build.parse import merge_records


def _rec(rid, applied, accepted, best, worst, avg, prefs):
    return {"id": rid, "sheet": "전형", "region": "경남", "university": "경상국립대",
            "type": "학생부교과", "name": "일반", "unit": None,
            "count": {"applied": applied, "accepted": accepted},
            "grades": {"best": best, "avg": avg, "median": avg, "worst": worst},
            "preferences": prefs}


def test_merge_combines_same_base_id():
    r1 = _rec("전형:경상국립대:학생부교과:일반", 3, 0, 1.13, 1.2, 1.16,
              [{"rank": 1, "university": "서울대", "label": "지역균형", "applied": 2, "accepted": 1}])
    r2 = _rec("전형:경상국립대:학생부교과:일반#2", 19, 7, 1.34, 5.52, 2.0,
              [{"rank": 1, "university": "서울대", "label": "지역균형", "applied": 3, "accepted": 0},
               {"rank": 2, "university": "연세대", "label": "추천형", "applied": 1, "accepted": 1}])
    out = merge_records([r1, r2])
    assert len(out) == 1
    m = out[0]
    assert m["id"] == "전형:경상국립대:학생부교과:일반"
    assert m["count"] == {"applied": 22, "accepted": 7}
    assert m["grades"]["best"] == 1.13
    assert m["grades"]["worst"] == 5.52
    assert m["grades"]["avg"] == round((1.16 * 3 + 2.0 * 19) / 22, 4)
    p = m["preferences"]
    assert p[0] == {"rank": 1, "university": "서울대", "label": "지역균형", "applied": 5, "accepted": 1}
    assert p[1] == {"rank": 2, "university": "연세대", "label": "추천형", "applied": 1, "accepted": 1}


def test_different_base_id_not_merged():
    # 캠퍼스가 다르면(대학명·지역 다름) 병합되지 않음
    a = _rec("전형:경희대:학생부종합:네오르네상스", 45, 8, 1.03, 3.36, 1.5, [])
    b = _rec("전형:경희대(국):학생부종합:네오르네상스", 10, 2, 2.0, 3.0, 2.5, [])
    b["university"] = "경희대(국)"; b["region"] = "경기"
    out = merge_records([a, b])
    assert len(out) == 2


def test_single_record_passes_through():
    r = _rec("전형:서울대:학생부종합:지역균형", 94, 33, 1.12, 1.45, 1.24,
             [{"rank": 1, "university": "고려대", "label": "학교추천", "applied": 60, "accepted": 44}])
    out = merge_records([r])
    assert len(out) == 1
    assert out[0]["id"] == "전형:서울대:학생부종합:지역균형"
    assert out[0]["preferences"][0]["applied"] == 60


def test_merge_strips_only_trailing_hash_number():
    # '#2' 같은 접미만 제거, 본문 내 # 은 보존(방어)
    out = merge_records([_rec("전형:경상국립대:학생부교과:일반#3", 1, 0, 1.5, 1.5, 1.5, [])])
    assert out[0]["id"] == "전형:경상국립대:학생부교과:일반"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd "C:/Users/user/Documents/claude/another" && python -m pytest build/tests/test_merge.py -v`
Expected: FAIL — `ImportError: cannot import name 'merge_records'`

- [ ] **Step 3: 최소 구현 — `build/parse.py` 끝에 APPEND**

```python
def _base_id(rid):
    """레코드 id 끝의 '#숫자' 접미 제거 → 병합 그룹 키."""
    return re.sub(r"#\d+$", "", rid)


def _weighted_avg(pairs):
    """pairs: [(value, weight)]. value None 은 제외. 전부 None 이면 None."""
    num = 0.0
    den = 0
    for v, w in pairs:
        if v is None:
            continue
        num += v * w
        den += w
    return round(num / den, 4) if den else None


def merge_records(records):
    """base-id 가 같은 레코드들을 한 레코드로 완전 병합.

    - count: applied/accepted 합산
    - grades: best=min, worst=max, avg/median=applied 가중평균
    - preferences: 정규화 대상(대학|전형) 기준 합산 → applied 내림차순 재정렬·재순위
    matchedId/rate 는 설정하지 않음(호출 측이 병합 후 수행).
    입력 순서를 보존한다.
    """
    groups = {}
    order = []
    for r in records:
        bid = _base_id(r["id"])
        if bid not in groups:
            groups[bid] = []
            order.append(bid)
        groups[bid].append(r)

    merged = []
    for bid in order:
        grp = groups[bid]
        if len(grp) == 1:
            r = dict(grp[0])
            r["id"] = bid
            merged.append(r)
            continue
        first = grp[0]
        applied = sum(r["count"]["applied"] for r in grp)
        accepted = sum(r["count"]["accepted"] for r in grp)
        bests = [r["grades"]["best"] for r in grp if r["grades"]["best"] is not None]
        worsts = [r["grades"]["worst"] for r in grp if r["grades"]["worst"] is not None]
        avg = _weighted_avg([(r["grades"]["avg"], r["count"]["applied"]) for r in grp])
        median = _weighted_avg([(r["grades"]["median"], r["count"]["applied"]) for r in grp])

        agg = {}
        porder = []
        for r in grp:
            for p in r["preferences"]:
                k = normalize_university(p["university"]) + "|" + normalize_name(p["label"])
                if k not in agg:
                    agg[k] = {"university": p["university"], "label": p["label"],
                              "applied": 0, "accepted": 0}
                    porder.append(k)
                agg[k]["applied"] += p["applied"]
                agg[k]["accepted"] += p["accepted"]
        prefs = [agg[k] for k in porder]
        prefs.sort(key=lambda x: x["applied"], reverse=True)
        for i, p in enumerate(prefs, start=1):
            p["rank"] = i

        merged.append({
            "id": bid,
            "sheet": first["sheet"],
            "region": first["region"],
            "university": first["university"],
            "type": first["type"],
            "name": first["name"],
            "unit": first["unit"],
            "count": {"applied": applied, "accepted": accepted},
            "grades": {
                "best": min(bests) if bests else None,
                "avg": avg,
                "median": median,
                "worst": max(worsts) if worsts else None,
            },
            "preferences": prefs,
        })
    return merged
```

참고: `merge_records`는 `normalize_university`, `normalize_name`(이미 parse.py에 정의됨)을 사용한다. `re` 도 이미 import 되어 있다.

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest build/tests/test_merge.py -v`
Expected: 4 passed
전체: `python -m pytest build/tests/ -v` → 모두 통과(기존 16 + 4 = 20).

- [ ] **Step 5: 커밋**

```bash
git add build/parse.py build/tests/test_merge.py
git commit -m "feat: merge_records — 표시상 동일 전형 완전 병합

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: 빌드에 병합 연결 + 재빌드 검증

**Files:**
- Modify: `build/build.py`

- [ ] **Step 1: import 에 merge_records 추가**

`build/build.py` 의 import 줄을 변경:
```python
from build.parse import parse_sheet, build_match_index, find_match
```
→
```python
from build.parse import parse_sheet, build_match_index, find_match, merge_records
```

- [ ] **Step 2: parse 직후 병합 호출**

`records, skipped = parse_sheet(sheet_name, rows[h:])` 바로 다음 줄에 추가:
```python
        records = merge_records(records)
```
(이후 `build_match_index(records)` 및 매칭 루프·출력은 그대로 — 병합 레코드에 그대로 적용됨.)

- [ ] **Step 3: 재빌드 + 유일성·행수 확인**

```bash
cd "C:/Users/user/Documents/claude/another" && python build/build.py
python -c "
import json
for key in ['jeonhyeong','jeonhyeong_inmun','jeonhyeong_jayeon','hakgwa']:
    s=open(f'web/data/{key}.js',encoding='utf-8').read().strip()
    data=json.loads(s[s.rindex('=')+1:].rstrip(';'))
    ids=[r['id'] for r in data]
    has_hash=any('#' in i for i in ids)
    print(key,'records=',len(data),'unique=',len(set(ids)),'hash_suffix=',has_hash)
"
```
Expected:
- `BUILD DONE` 출력에서 전형 행수가 1900 → 약 1714 로 감소(인문/자연도 감소, 학과는 거의 동일).
- 위 파이썬: 모든 시트 `records==unique` 이고 `hash_suffix= False`(병합으로 `#n` 사라짐).
실제 출력 숫자를 기록할 것.

- [ ] **Step 4: 병합 결과 표본 검증**

```bash
python -c "
import json
s=open('web/data/jeonhyeong.js',encoding='utf-8').read().strip()
data=json.loads(s[s.rindex('=')+1:].rstrip(';'))
m=[r for r in data if r['university']=='경상국립대' and r['type']=='학생부교과' and r['name']=='일반']
print('경상국립대 학생부교과 일반 레코드 수:', len(m))
r=m[0]
print('사례수:', r['count'], '내신:', r['grades']['best'],'~',r['grades']['worst'])
print('지망 top3:', [(p['rank'],p['university'],p['label'],p['applied']) for p in r['preferences'][:3]])
"
```
Expected: 레코드 수 **1**(5개가 1개로 병합), 사례수 applied=66(=3+19+31+12+1), 내신 best≈1.13 worst≈6.64, 지망은 합산·재순위된 목록.

- [ ] **Step 5: 커밋 (web/data 는 이제 git 추적 대상)**

```bash
git add build/build.py web/data
git commit -m "build: 병합 적용 + 데이터 재생성

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: 브라우저 실측 + 배포

**Files:** (없음 — 검증/배포만)

- [ ] **Step 1: 로컬 미리보기에서 중복 사라짐 확인**

미리보기 서버(`.claude/launch.json` 의 `web`, 포트 8765)를 띄우고 브라우저에서 `http://localhost:8765` 새로고침.
확인:
- 전형 탭에서 `경희대` 검색 → `서울 경희대 학생부종합 네오르네상스` 가 **1줄만** 표시
- `경상국립대` 검색 → `학생부교과 일반` 1줄, 클릭 시 사례수 66, 내신 범위, 합쳐진 같이 지원한 전형 목록
- 콘솔 에러 없음
- 캠퍼스 구분(`경희대` vs `경희대(국)`)은 여전히 별도 표시(정상)

- [ ] **Step 2: 푸시 → GitHub Pages 자동 재배포**

```bash
git push origin main
```
GitHub Actions 워크플로가 자동 실행됨. 1~2분 후 완료.

- [ ] **Step 3: 라이브 사이트 확인**

```bash
python -c "import urllib.request; print(urllib.request.urlopen('https://specialzoker.github.io/another/').status)"
```
Expected: 200. 브라우저에서 https://specialzoker.github.io/another/ 의 `경희대` 검색이 병합된 결과를 보이는지 확인(배포 전파에 1~2분 걸릴 수 있음).

---

## Self-Review (작성자 점검)
- **스펙 커버리지**: 병합 단위(base-id)·병합 규칙(count 합산/grades 범위·가중평균/preferences 합산 재순위)·빌드 순서(parse→merge→match→출력)·캠퍼스 분리·테스트·브라우저 실측·재배포 모두 Task 1–3 에 대응.
- **플레이스홀더**: 없음(모든 코드·명령·기대값 명시).
- **타입 일관성**: `merge_records`가 내보내는 레코드 키(id, sheet, region, university, type, name, unit, count{applied,accepted}, grades{best,avg,median,worst}, preferences[{rank,university,label,applied,accepted}])는 `build.py` 의 매칭 루프·index 생성·프런트 소비와 일치. matchedId/rate 는 병합 후 build.py 가 부여(기존 로직 그대로).
- **의도된 한계**: 가중평균에 median 도 동일 적용(미표시 필드라 무해).
