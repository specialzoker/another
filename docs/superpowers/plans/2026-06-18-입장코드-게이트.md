# 입장코드 게이트 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 박스플롯 앱과 동일한 Supabase `validate-key` 기반 입장 게이트를 추가해, 유효한 학교 키(또는 관리자 로그인) 통과 후에만 앱이 보이게 한다. 한 번 통과하면 localStorage로 기억한다.

**Architecture:** 새 `web/auth.js`가 게이트 로직을 담고, `index.html`에 게이트 오버레이를 추가하며 `body.locked`로 시작한다. `app.js`는 자동 init 대신 `window.App.start()`를 노출하고, 게이트 통과 시 호출된다. 소프트 게이트(화면만 가림, 정적 데이터는 공개 유지).

**Tech Stack:** Vanilla JS, Supabase REST/Edge Function(publishable 공개키), localStorage. 프런트 전용. 빌드 불필요. 커밋 → GitHub Pages 자동 재배포. (프런트라 단위 테스트 없음 — 브라우저 실측으로 검증.)

---

## File Structure
- `web/app.js` — 자동 init 제거, `window.App.start = init` 노출
- `web/auth.js` (신규) — 게이트 로직(Supabase 검증·관리자 로그인·비번재설정·remember·logout)
- `web/index.html` — 게이트 오버레이 markup, `body class="locked"`, auth.js 로드, 헤더에 로그아웃·학교태그
- `web/styles.css` — 게이트 스타일(앱 톤)

참고 소스: 박스플롯 입장 로직은 `https://specialzoker.github.io/ggjinhyubboxplot/` 에서 확인됨. 임시로 받아둔 `_boxplot_source.html`는 작업 후 삭제하고 절대 커밋하지 않는다.

---

## Task 1: 입장 게이트 구현 (4개 파일)

**Files:** Modify `web/app.js`, `web/index.html`, `web/styles.css`; Create `web/auth.js`

- [ ] **Step 1: app.js — 자동 init 제거 + start 노출**

`web/app.js` 끝부분의 이 줄:
```js
  document.addEventListener('DOMContentLoaded', init);
```
을 다음으로 교체:
```js
  window.App.start = init;
```
(다른 부분은 그대로. `init`은 함수 선언이라 호이스팅되어 정상 동작. 이제 게이트 통과 전엔 앱이 구동되지 않음.)

- [ ] **Step 2: auth.js 생성** — `web/auth.js`:

```js
// 입장 게이트 — 박스플롯 앱과 동일한 Supabase 검증 방식.
window.Auth = (function(){
  const SUPABASE_URL = 'https://qctffhrkhuaquciinblj.supabase.co';
  const SUPABASE_KEY = 'sb_publishable_4FZ0r-MqFdCjN4a1tRvLIQ_UmJVx0mj';
  const EDGE_FN_URL  = SUPABASE_URL + '/functions/v1/validate-key';
  const STORE_KEY    = 'gate_entry';

  let _adminMode = false, _adminToken = null;

  function $(id){ return document.getElementById(id); }
  function setStatus(msg){ var e=$('gateStatus'); if(e) e.textContent = msg||''; }
  function setErr(msg){ var e=$('gateErr'); if(e) e.textContent = msg||''; }

  function reveal(name){
    try{ localStorage.setItem(STORE_KEY, JSON.stringify({name:name||'', ts:Date.now()})); }catch(e){}
    document.body.classList.remove('locked');
    var tag=$('schoolTag'); if(tag) tag.textContent = name||'';
    if(window.App && typeof window.App.start==='function' && !window.App._started){
      window.App._started = true;
      window.App.start();
    }
  }

  function togglePw(){
    var inp=$('keyInput'); inp.type = inp.type==='password'?'text':'password';
  }

  function toggleAdminMode(){
    _adminMode = !_adminMode;
    $('keyWrap').classList.toggle('hidden', _adminMode);
    $('adminWrap').classList.toggle('hidden', !_adminMode);
    $('gateBtn').textContent = _adminMode ? '관리자 로그인' : '확인';
    $('gateBtn').onclick = _adminMode ? doAdminLogin : doLogin;
    $('adminToggle').textContent = _adminMode ? '← 학교 키 로그인' : '관리자 로그인';
    $('gateSub').textContent = _adminMode ? '관리자 이메일과 비밀번호를 입력하세요' : '학교 고유 키를 입력하세요';
    setErr(''); setStatus('');
  }

  async function doLogin(){
    var key = $('keyInput').value.trim();
    setErr('');
    if(!key){ setErr('키를 입력해주세요.'); return; }
    try{
      setStatus('⏳ 확인 중...');
      var res = await fetch(EDGE_FN_URL, {
        method:'POST', headers:{'Content-Type':'application/json'},
        body: JSON.stringify({ key: key })
      });
      var data = await res.json();
      if(!res.ok){ setErr(data.error || '유효하지 않은 키입니다.'); setStatus(''); return; }
      reveal(data.school ? data.school.sch_name : '');
    }catch(e){ setErr('연결 오류: ' + e.message); setStatus(''); }
  }

  async function doAdminLogin(){
    var email = $('adminEmail').value.trim();
    var pw = $('adminPw').value;
    setErr('');
    if(!email || !pw){ setErr('이메일과 비밀번호를 입력해주세요.'); return; }
    try{
      setStatus('⏳ 관리자 인증 중...');
      var res = await fetch(SUPABASE_URL + '/auth/v1/token?grant_type=password', {
        method:'POST', headers:{'Content-Type':'application/json','apikey':SUPABASE_KEY},
        body: JSON.stringify({ email: email, password: pw })
      });
      if(!res.ok){ setErr('이메일 또는 비밀번호가 올바르지 않습니다.'); setStatus(''); return; }
      var authData = await res.json();
      _adminToken = authData.access_token;
      reveal('관리자');
    }catch(e){ setErr('연결 오류: ' + e.message); setStatus(''); }
  }

  async function doResetPw(){
    var pw=$('newPw').value, pw2=$('newPw2').value, err=$('resetErr');
    err.textContent='';
    if(pw.length<6){ err.textContent='비밀번호는 6자 이상이어야 합니다.'; return; }
    if(pw!==pw2){ err.textContent='비밀번호가 일치하지 않습니다.'; return; }
    try{
      var res = await fetch(SUPABASE_URL + '/auth/v1/user', {
        method:'PUT',
        headers:{'Content-Type':'application/json','apikey':SUPABASE_KEY,'Authorization':'Bearer '+_adminToken},
        body: JSON.stringify({ password: pw })
      });
      if(!res.ok){ err.textContent='변경 실패. 링크가 만료되었을 수 있습니다.'; return; }
      $('resetView').classList.add('hidden'); $('loginView').classList.remove('hidden');
      _adminToken=null; alert('✅ 비밀번호가 변경되었습니다. 다시 로그인해주세요.');
    }catch(e){ err.textContent='오류: '+e.message; }
  }

  function logout(){
    try{ localStorage.removeItem(STORE_KEY); }catch(e){}
    location.reload();
  }

  function checkRecovery(){
    var hash = new URLSearchParams(window.location.hash.substring(1));
    if(hash.get('type')==='recovery'){
      _adminToken = hash.get('access_token');
      $('resetView').classList.remove('hidden');
      $('loginView').classList.add('hidden');
      history.replaceState(null,'',window.location.pathname);
    }
  }

  function boot(){
    checkRecovery();
    var saved=null;
    try{ saved = JSON.parse(localStorage.getItem(STORE_KEY)||'null'); }catch(e){}
    if(saved){ reveal(saved.name); }   // 기억된 입장 → 게이트 스킵
  }

  document.addEventListener('DOMContentLoaded', boot);

  return { doLogin: doLogin, doAdminLogin: doAdminLogin, toggleAdminMode: toggleAdminMode,
           togglePw: togglePw, doResetPw: doResetPw, logout: logout };
})();
```

- [ ] **Step 3: index.html — 게이트 markup + body.locked + auth.js 로드 + 헤더 로그아웃/학교태그**

(a) `<body>` 를 `<body class="locked">` 로 변경.

(b) `<body class="locked">` 바로 다음(헤더 앞)에 게이트 오버레이 삽입:
```html
<div id="gate">
  <div class="gateCard">
    <div id="resetView" class="hidden">
      <div class="gateLogo">진협</div>
      <div class="gateTitle">비밀번호 재설정</div>
      <div class="gateSub">새 비밀번호를 입력하세요</div>
      <div class="gateField"><input type="password" id="newPw" placeholder="새 비밀번호 (6자 이상)" onkeydown="if(event.key==='Enter')Auth.doResetPw()"></div>
      <div class="gateField"><input type="password" id="newPw2" placeholder="비밀번호 확인" onkeydown="if(event.key==='Enter')Auth.doResetPw()"></div>
      <button class="gateBtn" onclick="Auth.doResetPw()">비밀번호 변경</button>
      <div class="gateErr" id="resetErr"></div>
    </div>
    <div id="loginView">
      <div class="gateLogo">진협</div>
      <div class="gateTitle">2026 지원경향 분석</div>
      <div class="gateSub" id="gateSub">학교 고유 키를 입력하세요</div>
      <div id="keyWrap">
        <div class="gateField">
          <input type="password" id="keyInput" placeholder="학교 고유 키 입력" onkeydown="if(event.key==='Enter')Auth.doLogin()">
          <button type="button" class="gateToggle" onclick="Auth.togglePw()">표시</button>
        </div>
      </div>
      <div id="adminWrap" class="hidden">
        <div class="gateField"><input type="email" id="adminEmail" placeholder="관리자 이메일" onkeydown="if(event.key==='Enter')Auth.doAdminLogin()"></div>
        <div class="gateField"><input type="password" id="adminPw" placeholder="관리자 비밀번호" onkeydown="if(event.key==='Enter')Auth.doAdminLogin()"></div>
      </div>
      <button class="gateBtn" id="gateBtn" onclick="Auth.doLogin()">확인</button>
      <div class="gateErr" id="gateErr"></div>
      <div class="gateStatus" id="gateStatus"></div>
      <div style="margin-top:12px"><button onclick="Auth.toggleAdminMode()" id="adminToggle" class="gateLink">관리자 로그인</button></div>
    </div>
  </div>
</div>
```

(c) 헤더의 `.headerRight` 블록을 다음으로 교체(로그아웃·학교태그 추가):
```html
    <div class="headerRight">
      <div class="topBadge" id="reportBadge">로딩…</div>
      <div class="credit">제작 · 광명북고 진로진학부장 정현석</div>
      <div style="display:flex;align-items:center;gap:8px">
        <span id="schoolTag"></span>
        <button class="logoutBtn" onclick="Auth.logout()">로그아웃</button>
      </div>
    </div>
```

(d) 스크립트 로드부에 auth.js 추가(app.js 다음):
```html
<script src="data/index.js"></script>
<script src="render.js"></script>
<script src="app.js"></script>
<script src="auth.js"></script>
```

- [ ] **Step 4: styles.css — 게이트 스타일 추가 (파일 끝에 APPEND)**

```css
/* ===== 입장 게이트 ===== */
body.locked > header, body.locked > main{display:none}
#gate{display:none;position:fixed;inset:0;align-items:center;justify-content:center;background:linear-gradient(180deg,#f8fbff,#eef4fb);padding:20px;z-index:1000}
body.locked #gate{display:flex}
.gateCard{background:#fff;border:1px solid var(--line);border-radius:20px;box-shadow:0 12px 40px rgba(15,23,42,.12);padding:32px 28px;width:100%;max-width:380px;text-align:center}
.gateLogo{width:54px;height:54px;border-radius:14px;background:linear-gradient(135deg,#1d4ed8,#0f3b75);color:#fff;font-weight:900;display:flex;align-items:center;justify-content:center;margin:0 auto 14px;font-size:18px}
.gateTitle{font-size:20px;font-weight:800;color:#06234a;margin-bottom:6px}
.gateSub{font-size:13px;color:#607089;margin-bottom:18px}
.gateField{position:relative;margin-bottom:10px}
.gateField input{width:100%;border:1px solid var(--line);border-radius:10px;padding:11px 12px;font-size:14px}
.gateToggle{position:absolute;right:8px;top:50%;transform:translateY(-50%);background:#eef2ff;border:0;border-radius:8px;padding:5px 9px;font-size:12px;font-weight:700;color:#3730a3;cursor:pointer}
.gateBtn{width:100%;margin-top:6px;background:linear-gradient(135deg,#1d4ed8,#0f3b75);color:#fff;border:0;border-radius:10px;padding:12px;font-weight:800;font-size:15px;cursor:pointer}
.gateErr{color:#b91c1c;font-size:13px;font-weight:700;margin-top:10px;min-height:18px}
.gateStatus{color:#31527a;font-size:12px;margin-top:8px;min-height:16px}
.gateLink{background:none;border:0;font-size:12px;color:#64748b;cursor:pointer;text-decoration:underline}
.logoutBtn{background:#f1f5f9;border:1px solid var(--line);border-radius:8px;padding:5px 10px;font-size:12px;font-weight:700;color:#475569;cursor:pointer}
#schoolTag{font-size:12px;color:#0f3b75;font-weight:800}
```

- [ ] **Step 5: 정적 점검 + 임시 소스 삭제**

```bash
cd "C:/Users/user/Documents/claude/another"
rm -f _boxplot_source.html
ls web/auth.js && grep -c "Auth.doLogin" web/index.html && grep -c "window.App.start" web/app.js
```
Expected: auth.js 존재, index.html에 `Auth.doLogin` 참조 있음, app.js에 `window.App.start` 있음. `_boxplot_source.html` 삭제됨(커밋 금지).

- [ ] **Step 6: 커밋**

```bash
git add web/app.js web/auth.js web/index.html web/styles.css
git commit -m "feat: 입장코드 게이트(Supabase validate-key, 박스플롯 동일 방식)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```
(주의: `_boxplot_source.html` 은 add 하지 말 것.)

---

## Task 2: 브라우저 실측 + 배포 (컨트롤러 수행)

**Files:** (없음 — 검증/배포)

- [ ] **Step 1: 게이트 표시 확인**

미리보기(`.claude/launch.json` 의 `web`, 8765) 새로고침. localStorage 비운 상태에서:
- 헤더·본문이 숨겨지고 **게이트 카드만** 보임("학교 고유 키를 입력하세요").
- `body.classList.contains('locked') === true`.

- [ ] **Step 2: 입력 검증/에러 경로**

- 빈 키로 확인 → "키를 입력해주세요." 표시.
- 임의의 잘못된 키 입력 → (Supabase 도달 시) validate-key 에러 문구, 또는 (이 PC가 supabase.co 차단 시) "연결 오류: ..." — 둘 다 게이트 유지가 정상. Supabase 도달 여부를 콘솔/네트워크로 기록.
- 관리자 토글 → 이메일·비번 칸으로 전환, 버튼 텍스트 '관리자 로그인'.

- [ ] **Step 3: 통과(reveal) 동작 — 모의로 확인**

실제 유효키가 없으므로 reveal 경로를 직접 호출해 앱 노출을 확인:
```js
// preview_eval
(function(){ localStorage.removeItem('gate_entry'); document.body.classList.add('locked');
  // reveal 내부 동작과 동일하게: 잠금 해제 + 앱 시작
  document.body.classList.remove('locked');
  if(window.App && App.start && !App._started){ App._started=true; App.start(); }
  return {locked:document.body.classList.contains('locked'), started:!!App._started}; })()
```
Expected: `locked:false, started:true`, 잠시 후 탭·검색·결과가 정상 렌더(앱 구동 확인).

- [ ] **Step 4: 기억(localStorage) + 로그아웃**

- `localStorage.setItem('gate_entry', JSON.stringify({name:'테스트고', ts:Date.now()}))` 후 새로고침 → 게이트 스킵하고 바로 앱, 헤더 `#schoolTag`에 '테스트고'.
- 로그아웃 버튼 클릭 → localStorage 삭제 + 리로드 → 다시 게이트 표시.
- 콘솔 에러 0 확인.

- [ ] **Step 5: 배포**

```bash
git push origin main
```
GitHub Actions 자동 재배포(1~2분). 완료 후 라이브에서 게이트가 뜨는지 확인:
```bash
python -c "import urllib.request; h=urllib.request.urlopen('https://specialzoker.github.io/another/auth.js').status; print('auth.js', h)"
```
Expected: 200. **실제 유효 학교 키로의 입장 성공은 제작자(사용자)가 최종 확인** — 제가 보유한 키가 없음.

---

## Self-Review (작성자 점검)
- **스펙 커버리지**: 게이트 오버레이·body.locked(Step3,4) / auth.js Supabase 검증·관리자 로그인·비번재설정·remember·logout(Step2) / app.js start 노출(Step1) / 앱 톤 스타일(Step4) / 소프트 게이트 한계·임시소스 삭제(Step5) / 브라우저·배포 검증(Task2) 모두 대응.
- **플레이스홀더**: 없음(모든 코드·명령 명시).
- **타입/식별자 일관성**: HTML id(keyInput, adminEmail, adminPw, gateBtn, gateSub, gateErr, gateStatus, adminToggle, keyWrap, adminWrap, resetView, loginView, resetErr, newPw, newPw2, schoolTag)와 auth.js의 `$()` 참조, `window.App.start`/`_started`(app.js↔auth.js), 스크립트 로드 순서 일치.
- **보안 한계**: 소프트 게이트(데이터 파일 공개 유지), publishable 공개키 재사용 — 스펙과 일치.
