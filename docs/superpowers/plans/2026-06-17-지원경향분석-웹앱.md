# 지원경향분석 웹앱 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 작년 입시 교차지원 경향(어떤 전형·학과 지원자가 또 어디에 지원·합격했는지)을 검색·순위표·드릴인으로 보여주는 정적 웹앱을 만든다.

**Architecture:** 파이썬 빌드 스크립트가 엑셀 4개 시트를 파싱해 `data/*.js`(전역변수 주입) 산출물을 만들고, 서버 없는 정적 프런트엔드(`web/`)가 `<script>` 태그로 이를 로드해 `file://` 더블클릭으로도 동작한다. 복잡 로직(셀 파싱·정규화·역방향 매칭)은 파이썬에서 pytest로 TDD, 프런트엔드는 브라우저 실측으로 검증한다.

**Tech Stack:** Python 3.12 + openpyxl (빌드/테스트: pytest), 의존성 없는 HTML/CSS/Vanilla JS, 순수 SVG 허브-스포크 다이어그램.

---

## File Structure

```
project/
├─ build/
│  ├─ parse.py          # 순수 함수: 셀 파싱, 정규화, 매칭, 시트→records
│  ├─ build.py          # CLI: 엑셀 읽어 web/data/*.js 산출 + 리포트
│  └─ tests/
│     ├─ test_cell.py        # 셀 파싱 테스트
│     ├─ test_normalize.py   # 정규화/매칭 테스트
│     └─ test_sheet.py       # 시트→records 테스트(픽스처)
├─ web/
│  ├─ index.html        # 단일 진입점, 화면 컨테이너
│  ├─ styles.css        # 디자인 토큰(기존 상담앱 톤)
│  ├─ app.js            # 화면 전환·검색·렌더링·이벤트
│  ├─ render.js         # 순수 렌더 헬퍼(순위표/카드/SVG 다이어그램)
│  └─ data/             # build.py 산출물(생성)
│     ├─ index.js       # window.__APPINDEX__ = [...]
│     ├─ jeonhyeong.js  # window.__APPDATA__['jeonhyeong'] = [...]
│     ├─ jeonhyeong_inmun.js
│     ├─ jeonhyeong_jayeon.js
│     ├─ hakgwa.js
│     └─ report.json    # 빌드 리포트(행수·매칭률·스킵)
└─ docs/superpowers/...
```

원본 엑셀: `C:\클라우드파일\다운 받은 파일\2026\2026 지원경향분석_전형_학과.xlsx`
시트 키 매핑: `전형→jeonhyeong`, `전형_인문→jeonhyeong_inmun`, `전형_자연→jeonhyeong_jayeon`, `학과→hakgwa`.

---

## Task 1: 프로젝트 초기화 & 셀 파싱 함수

**Files:**
- Create: `build/parse.py`
- Test: `build/tests/test_cell.py`

- [ ] **Step 1: 저장소·환경 준비**

```bash
cd "C:/Users/user/Documents/claude/another"
git init
printf "__pycache__/\n*.pyc\n.pytest_cache/\nweb/data/\n" > .gitignore
python -m pip install openpyxl pytest
mkdir -p build/tests web/data
```

- [ ] **Step 2: 실패하는 테스트 작성**

`build/tests/test_cell.py`:

```python
from build.parse import parse_pref_cell

def test_simple_cell():
    # 전형 시트: 대학 / (전형) / N[M]
    cell = "경상국립대_x000D_\n(일반)_x000D_\n1[0]"
    assert parse_pref_cell(cell) == {
        "university": "경상국립대", "label": "일반",
        "applied": 1, "accepted": 0,
    }

def test_count_with_spaces():
    cell = "고려대_x000D_\n학교추천_x000D_\n60 [44]"
    assert parse_pref_cell(cell) == {
        "university": "고려대", "label": "학교추천",
        "applied": 60, "accepted": 44,
    }

def test_merged_univ_track():
    # 줄바꿈 없이 대학+전형이 붙은 경우 → label은 빈 문자열, 대학명에 전체
    cell = "영남대일반학생_x000D_\n1[0]"
    assert parse_pref_cell(cell) == {
        "university": "영남대일반학생", "label": "",
        "applied": 1, "accepted": 0,
    }

def test_hakgwa_three_lines():
    # 학과 시트: 대학 / (전형) / 학과 / N[M] → label은 " · "로 결합
    cell = "가톨릭대_x000D_\n(지역균형)_x000D_\n의예과_x000D_\n1[1]"
    assert parse_pref_cell(cell) == {
        "university": "가톨릭대", "label": "지역균형 · 의예과",
        "applied": 1, "accepted": 1,
    }

def test_empty_or_malformed_returns_none():
    assert parse_pref_cell(None) is None
    assert parse_pref_cell("") is None
    assert parse_pref_cell("그냥텍스트_x000D_\n없음") is None  # 마지막 줄 N[M] 아님
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `cd "C:/Users/user/Documents/claude/another" && python -m pytest build/tests/test_cell.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'build.parse'` (또는 함수 없음)

- [ ] **Step 4: 최소 구현**

`build/parse.py`:

```python
import re

_COUNT_RE = re.compile(r"^\s*(\d+)\s*\[\s*(\d+)\s*\]\s*$")


def _clean_lines(cell):
    """셀 문자열을 줄 리스트로 정리. _x000D_ 와 개행 제거."""
    text = cell.replace("_x000D_", "")
    lines = [ln.strip() for ln in text.replace("\r", "\n").split("\n")]
    return [ln for ln in lines if ln]


def parse_pref_cell(cell):
    """지망 칸 1개를 파싱. 형식 불일치면 None.

    반환: {university, label, applied, accepted}
    - 마지막 줄이 N[M] 이어야 유효.
    - 첫 줄 = 대학명, 가운데 줄들 = 전형명/학과명(" · "로 결합), 괄호는 제거.
    """
    if not cell or not isinstance(cell, str):
        return None
    lines = _clean_lines(cell)
    if len(lines) < 2:
        return None
    m = _COUNT_RE.match(lines[-1])
    if not m:
        return None
    applied, accepted = int(m.group(1)), int(m.group(2))
    university = lines[0]
    middle = lines[1:-1]
    label_parts = [_strip_parens(p) for p in middle]
    label = " · ".join(p for p in label_parts if p)
    return {
        "university": university,
        "label": label,
        "applied": applied,
        "accepted": accepted,
    }


def _strip_parens(s):
    """앞뒤 괄호류 제거: (일반)->일반, [면접]->면접. 내부는 보존."""
    s = s.strip()
    if len(s) >= 2 and s[0] in "([" and s[-1] in ")]":
        return s[1:-1].strip()
    return s
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m pytest build/tests/test_cell.py -v`
Expected: PASS (5 passed)

- [ ] **Step 6: 커밋**

```bash
git add .gitignore build/parse.py build/tests/test_cell.py
git commit -m "feat: 지망 셀 파싱 함수 + 테스트"
```

---

## Task 2: 이름 정규화 & 역방향 매칭

**Files:**
- Modify: `build/parse.py`
- Test: `build/tests/test_normalize.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`build/tests/test_normalize.py`:

```python
from build.parse import normalize_name, match_key, build_match_index, find_match

def test_normalize_removes_parens_and_spaces():
    assert normalize_name("(일반)") == "일반"
    assert normalize_name("학생부교과:학교추천") == "학생부교과:학교추천"
    assert normalize_name(" 추천형 전형 ") == "추천형"  # 공백·"전형" 접미 제거

def test_normalize_removes_campus_suffix():
    assert normalize_name("경상국립대칠암") == "경상국립대"
    assert normalize_name("인제대김해") == "인제대"
    assert normalize_name("전남대광주") == "전남대"

def test_match_key_combines_university_and_name():
    assert match_key("고려대", "학교추천") == "고려대|학교추천"

def test_find_match_best_effort():
    records = [
        {"id": "a", "university": "고려대", "type": "학생부교과", "name": "학생부교과:학교추천"},
        {"id": "b", "university": "연세대", "type": "학생부교과", "name": "추천형"},
    ]
    idx = build_match_index(records)
    # 지망 라벨 "학교추천" 은 원본 "학생부교과:학교추천" 에 부분일치로 매칭
    assert find_match(idx, "고려대", "학교추천") == "a"
    assert find_match(idx, "연세대", "추천형") == "b"
    assert find_match(idx, "서울대", "지역균형") is None  # 원본에 없음
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest build/tests/test_normalize.py -v`
Expected: FAIL — ImportError (함수 미정의)

- [ ] **Step 3: 최소 구현 (parse.py에 추가)**

`build/parse.py` 끝에 추가:

```python
_CAMPUS_SUFFIXES = ("칠암", "김해", "광주", "경산", "아산", "포항", "대구", "서울", "원주")


def normalize_name(s):
    """매칭용 정규화: 공백·괄호류·'전형' 접미·캠퍼스 접미 제거."""
    if not s:
        return ""
    s = s.strip()
    # 괄호류 전체 제거
    s = re.sub(r"[()\[\]]", "", s)
    # 끝의 '전형' 접미어 제거(반복)
    while s.endswith("전형"):
        s = s[:-2]
    s = s.replace(" ", "")
    return s


def normalize_university(s):
    """대학명 정규화 + 캠퍼스 접미 제거."""
    s = normalize_name(s)
    for suf in _CAMPUS_SUFFIXES:
        if len(s) > len(suf) + 1 and s.endswith(suf):
            return s[: -len(suf)]
    return s


def match_key(university, name):
    return f"{normalize_university(university)}|{normalize_name(name)}"


def build_match_index(records):
    """원본 records → {정규화대학: [(정규화전형명, id)]} 인덱스."""
    idx = {}
    for r in records:
        u = normalize_university(r["university"])
        n = normalize_name(r.get("name") or "")
        idx.setdefault(u, []).append((n, r["id"]))
    return idx


def find_match(idx, university, label):
    """지망 (대학,라벨) → 원본 id (best-effort). 실패 시 None.
    - 같은 대학 후보 중 정규화 라벨이 서로 부분문자열이면 매칭.
    """
    u = normalize_university(university)
    target = normalize_name(label)
    cands = idx.get(u)
    if not cands or not target:
        return None
    # 1) 완전일치 우선
    for n, rid in cands:
        if n == target:
            return rid
    # 2) 부분일치(양방향 포함관계)
    for n, rid in cands:
        if n and (target in n or n in target):
            return rid
    return None
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest build/tests/test_normalize.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: 커밋**

```bash
git add build/parse.py build/tests/test_normalize.py
git commit -m "feat: 이름 정규화 + 역방향 매칭"
```

---

## Task 3: 시트 → records 변환

**Files:**
- Modify: `build/parse.py`
- Test: `build/tests/test_sheet.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`build/tests/test_sheet.py` (openpyxl 없이 행 리스트로 직접 테스트):

```python
from build.parse import parse_sheet

HEADER = ["지역","대학명","전형유형","전형명","사례수","최고","평균","중간","최저",
          "1 지망","2 지망","최저","교과","종합","논술","실기","기타"]

ROW = ["경남","인제대","학생부교과","의예,약학","2 [1]",1.08,1.11,1.11,1.14,
       "경상국립대_x000D_\n(일반)_x000D_\n1[0]",
       "서울대_x000D_\n(지역균형)_x000D_\n1[0]",
       "", "", "", "", "", ""]

def test_parse_sheet_builds_record():
    recs, skipped = parse_sheet("전형", [HEADER, ROW])
    assert len(recs) == 1
    r = recs[0]
    assert r["region"] == "경남"
    assert r["university"] == "인제대"
    assert r["type"] == "학생부교과"
    assert r["name"] == "의예,약학"
    assert r["unit"] is None
    assert r["count"] == {"applied": 2, "accepted": 1}
    assert r["grades"] == {"best":1.08,"avg":1.11,"median":1.11,"worst":1.14}
    assert r["id"] == "전형:인제대:학생부교과:의예,약학"
    assert len(r["preferences"]) == 2
    assert r["preferences"][0] == {
        "rank":1,"university":"경상국립대","label":"일반","applied":1,"accepted":0
    }

def test_hakgwa_sheet_has_unit():
    header = ["지역","대명","전형유형","전형명","모집단위","사례수",
              "최고","평균","중간","최저","1 지망","2 지망","인문","자연"]
    row = ["서울","중앙대","종합","CAU융합형인재","의학부(의예)","1 [0]",
           1,1,1,1,
           "가톨릭대_x000D_\n(지역균형)_x000D_\n의예과_x000D_\n1[1]","",
           "",""]
    recs, _ = parse_sheet("학과", [header, row])
    assert recs[0]["unit"] == "의학부(의예)"
    assert recs[0]["id"] == "학과:중앙대:종합:CAU융합형인재:의학부(의예)"
    assert recs[0]["preferences"][0]["label"] == "지역균형 · 의예과"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest build/tests/test_sheet.py -v`
Expected: FAIL — `parse_sheet` 미정의

- [ ] **Step 3: 최소 구현 (parse.py에 추가)**

```python
def _parse_count(val):
    """'94 [33]' → (94, 33). 실패 시 (None, None)."""
    if val is None:
        return None, None
    m = re.match(r"^\s*(\d+)\s*\[\s*(\d+)\s*\]\s*$", str(val))
    if not m:
        return None, None
    return int(m.group(1)), int(m.group(2))


def _to_float(val):
    try:
        return round(float(val), 4)
    except (TypeError, ValueError):
        return None


def parse_sheet(sheet_name, rows):
    """rows[0]=헤더, 이후 데이터행. (records, skipped_count) 반환.

    헤더에서 컬럼 위치를 찾는다(시트마다 '대학명'/'대명' 등 표기가 달라
    텍스트로 탐색). 지망 컬럼 = 헤더가 '<숫자>지망' 패턴.
    """
    header = [str(h).replace(" ", "") if h is not None else "" for h in rows[0]]

    def col(*names):
        for nm in names:
            if nm in header:
                return header.index(nm)
        return None

    c_region = col("지역")
    c_univ = col("대학명", "대명")
    c_type = col("전형유형")
    c_name = col("전형명")
    c_unit = col("모집단위")
    c_count = col("사례수")
    c_best = col("최고")
    c_avg = col("평균")
    c_median = col("중간", "중간값")
    # '최저'는 등급(첫 등장)과 표 끝 머리글에 중복 → 사례수 직후의 첫 '최저' 사용
    c_worst = next((i for i, h in enumerate(header) if h == "최저" and i > c_count), None)
    pref_cols = [i for i, h in enumerate(header) if re.match(r"^\d+지망$", h)]

    records, skipped = [], 0
    for row in rows[1:]:
        if c_univ is None or row[c_univ] in (None, ""):
            continue
        applied, accepted = _parse_count(row[c_count])
        if applied is None:
            skipped += 1
            continue
        unit = str(row[c_unit]).strip() if c_unit is not None and row[c_unit] else None
        prefs = []
        for rank, ci in enumerate(pref_cols, start=1):
            parsed = parse_pref_cell(row[ci]) if ci < len(row) else None
            if parsed:
                parsed["rank"] = rank
                prefs.append(parsed)
        region = str(row[c_region]).strip() if row[c_region] else ""
        university = str(row[c_univ]).strip()
        type_ = str(row[c_type]).strip() if row[c_type] else ""
        name = str(row[c_name]).strip() if row[c_name] else ""
        id_parts = [_sheet_key(sheet_name), university, type_, name]
        if unit:
            id_parts.append(unit)
        records.append({
            "id": ":".join(id_parts),
            "sheet": sheet_name,
            "region": region,
            "university": university,
            "type": type_,
            "name": name,
            "unit": unit,
            "count": {"applied": applied, "accepted": accepted},
            "grades": {
                "best": _to_float(row[c_best]) if c_best is not None else None,
                "avg": _to_float(row[c_avg]) if c_avg is not None else None,
                "median": _to_float(row[c_median]) if c_median is not None else None,
                "worst": _to_float(row[c_worst]) if c_worst is not None else None,
            },
            "preferences": prefs,
        })
    return records, skipped


def _sheet_key(sheet_name):
    return {
        "전형": "전형", "전형_인문": "전형", "전형_자연": "전형", "학과": "학과",
    }.get(sheet_name, sheet_name)
```

참고: `id`의 시트 접두는 가독성을 위해 `전형`/`학과`만 사용(인문·자연도 `전형`). 매칭은 시트별 인덱스로 별도 처리하므로 충돌 없음.

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest build/tests/test_sheet.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 전체 테스트 재확인 + 커밋**

```bash
python -m pytest build/tests/ -v
git add build/parse.py build/tests/test_sheet.py
git commit -m "feat: 시트 → records 변환"
```
Expected: 모든 테스트 PASS

---

## Task 4: 빌드 CLI (엑셀 → web/data/*.js + 리포트)

**Files:**
- Create: `build/build.py`

- [ ] **Step 1: 빌드 스크립트 작성**

`build/build.py`:

```python
"""엑셀 → web/data/*.js 산출 + 매칭 + 리포트.
실행: python build/build.py
"""
import json
import os
import openpyxl

from build.parse import parse_sheet, build_match_index, find_match

XLSX = r"C:\클라우드파일\다운 받은 파일\2026\2026 지원경향분석_전형_학과.xlsx"
OUT_DIR = os.path.join(os.path.dirname(__file__), "..", "web", "data")

SHEET_KEYS = {
    "전형": "jeonhyeong",
    "전형_인문": "jeonhyeong_inmun",
    "전형_자연": "jeonhyeong_jayeon",
    "학과": "hakgwa",
}


def read_rows(ws):
    return [[c.value for c in row] for row in ws.iter_rows()]


def write_js(path, varexpr):
    with open(path, "w", encoding="utf-8") as f:
        f.write(varexpr)


def main():
    os.makedirs(OUT_DIR, exist_ok=True)
    wb = openpyxl.load_workbook(XLSX, data_only=True)
    report = {"sheets": {}}
    index = []

    for sheet_name, key in SHEET_KEYS.items():
        ws = wb[sheet_name]
        rows = read_rows(ws)
        # 헤더 행 탐색: '사례수' 포함 행
        h = next((i for i, r in enumerate(rows)
                  if any(str(c).replace(" ", "") == "사례수" for c in r if c)), 0)
        records, skipped = parse_sheet(sheet_name, rows[h:])

        # 역방향 매칭(같은 시트 내)
        idx = build_match_index(records)
        matched = total = 0
        for r in records:
            for p in r["preferences"]:
                total += 1
                mid = find_match(idx, p["university"], p["label"])
                p["matchedId"] = mid
                p["rate"] = round(p["accepted"] / p["applied"], 3) if p["applied"] else 0
                if mid:
                    matched += 1
            c = r["count"]
            r["rate"] = round(c["accepted"] / c["applied"], 3) if c["applied"] else 0

        write_js(
            os.path.join(OUT_DIR, f"{key}.js"),
            "window.__APPDATA__=window.__APPDATA__||{};window.__APPDATA__['%s']=%s;"
            % (key, json.dumps(records, ensure_ascii=False)),
        )
        for r in records:
            index.append({
                "id": r["id"], "key": key, "region": r["region"],
                "university": r["university"], "type": r["type"],
                "name": r["name"], "unit": r["unit"],
            })
        report["sheets"][key] = {
            "records": len(records), "skipped": skipped,
            "prefs": total, "matched": matched,
            "match_rate": round(matched / total, 3) if total else 0,
        }

    write_js(
        os.path.join(OUT_DIR, "index.js"),
        "window.__APPINDEX__=%s;" % json.dumps(index, ensure_ascii=False),
    )
    with open(os.path.join(OUT_DIR, "report.json"), "w", encoding="utf-8") as f:
        json.dump(report, f, ensure_ascii=False, indent=2)

    print("BUILD DONE")
    for k, v in report["sheets"].items():
        print(f"  {k}: {v['records']} rows, skipped {v['skipped']}, "
              f"match {int(v['match_rate']*100)}%")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 빌드 실행 & 산출물 확인**

Run: `cd "C:/Users/user/Documents/claude/another" && python build/build.py`
Expected 출력 예:
```
BUILD DONE
  jeonhyeong: 350+ rows, skipped N, match 70%+
  ...
  hakgwa: 1900+ rows, skipped N, match 60%+
```

- [ ] **Step 3: 산출 JS 유효성 점검**

Run:
```bash
python -c "import json,re; s=open('web/data/index.js',encoding='utf-8').read(); j=json.loads(re.sub(r'^window.__APPINDEX__=|;$','',s.strip())); print('index entries:', len(j)); print(j[0])"
```
Expected: `index entries: 4000+` 와 첫 항목 딕셔너리 출력

- [ ] **Step 4: 커밋**

```bash
git add build/build.py
git commit -m "feat: 엑셀 빌드 CLI + 역방향 매칭 + 리포트"
```

---

## Task 5: 프런트엔드 뼈대 + 디자인 토큰 + 데이터 로더

**Files:**
- Create: `web/index.html`, `web/styles.css`, `web/app.js`, `web/render.js`

- [ ] **Step 1: index.html 작성**

`web/index.html`:

```html
<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>2026 지원경향 분석</title>
<link rel="preconnect" href="https://cdn.jsdelivr.net">
<link href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css" rel="stylesheet">
<link rel="stylesheet" href="styles.css">
</head>
<body>
<header>
  <div class="brandbar">
    <div class="brandText">
      <h1>2026 지원경향 분석</h1>
      <p>작년 교차지원 · 합격 경향 조회</p>
    </div>
    <div class="topBadge" id="reportBadge">로딩…</div>
  </div>
</header>
<main>
  <div class="tabs" id="sheetTabs"></div>
  <div class="card" id="searchCard">
    <div class="form-grid-2">
      <div>
        <label>검색 (대학·전형·학과)</label>
        <input id="q" placeholder="예: 서울대 지역균형">
      </div>
      <div>
        <label>지역</label>
        <select id="regionFilter"><option value="">전체</option></select>
      </div>
    </div>
    <div class="scroll" style="max-height:340px;margin-top:12px">
      <table><tbody id="resultBody"></tbody></table>
    </div>
  </div>
  <div id="detail"></div>
</main>
<!-- 데이터: file:// 에서도 동작하도록 script 태그로 주입 -->
<script src="data/index.js"></script>
<script src="render.js"></script>
<script src="app.js"></script>
</body>
</html>
```

- [ ] **Step 2: styles.css 작성 (기존 상담앱 톤)**

`web/styles.css`:

```css
:root{--bg:#f4f7fb;--card:#fff;--line:#dbe4ef;--brand:#1d4ed8;--navy:#072b55;--accent:#f97316;--text:#172033}
*{box-sizing:border-box}
body{margin:0;background:linear-gradient(180deg,#f8fbff 0%,#eef4fb 100%);font-family:Pretendard,'Noto Sans KR',Arial,sans-serif;color:var(--text)}
header{background:linear-gradient(135deg,#fff 0%,#f4f8ff 52%,#eaf2ff 100%);padding:20px 28px;border-bottom:1px solid #d6e2f2;box-shadow:0 10px 30px rgba(15,23,42,.08)}
.brandbar{display:flex;align-items:center;gap:24px;max-width:1280px;margin:0 auto}
.brandText h1{margin:0 0 4px;font-size:30px;letter-spacing:-1px;color:#06234a}
.brandText p{margin:0;color:#31527a;font-weight:700}
.topBadge{margin-left:auto;background:#fff;border:1px solid var(--line);border-radius:16px;padding:10px 16px;color:#0f3b75;font-weight:800;box-shadow:0 6px 20px rgba(15,23,42,.06)}
main{max-width:1280px;margin:20px auto;padding:0 16px}
.card{background:rgba(255,255,255,.96);border:1px solid var(--line);border-radius:20px;padding:18px;box-shadow:0 12px 30px rgba(15,23,42,.07);margin-bottom:18px}
.card h2{margin:0 0 12px;font-size:19px}
label{font-size:13px;color:#475569;font-weight:800;display:block;margin-bottom:6px}
input,select{width:100%;border:1px solid var(--line);border-radius:10px;padding:9px 10px;font-size:14px}
.form-grid-2{display:grid;grid-template-columns:1fr 220px;gap:10px}
.tabs{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:14px}
.tab{background:#eef2ff;color:#3730a3;border-radius:999px;padding:8px 14px;cursor:pointer;font-weight:800;border:0}
.tab.active{background:linear-gradient(135deg,#1d4ed8,#0f3b75);color:#fff}
table{width:100%;border-collapse:collapse;font-size:13px}
th,td{border-bottom:1px solid var(--line);padding:8px;vertical-align:middle;text-align:left}
th{background:#f8fafc;position:sticky;top:0}
.scroll{max-height:620px;overflow:auto;border:1px solid var(--line);border-radius:12px}
tr.clickable{cursor:pointer}
tr.clickable:hover{background:#f1f6ff}
.pill{display:inline-block;border-radius:999px;padding:3px 9px;font-size:12px;font-weight:800}
.good{background:#dcfce7;color:#166534}.ok{background:#dbeafe;color:#1e40af}
.warn{background:#fef3c7;color:#92400e}.bad{background:#fee2e2;color:#991b1b}.muted{background:#eef2f7;color:#64748b}
.kpis{display:grid;grid-template-columns:repeat(5,1fr);gap:10px;margin:8px 0 4px}
.kpi{background:#f8fafc;border:1px solid var(--line);border-radius:14px;padding:12px}
.kpi span{font-size:12px;color:#607089}.kpi strong{font-size:20px;display:block;color:#06234a}
.bar{height:9px;border-radius:999px;background:#e6eefb;overflow:hidden}
.bar>i{display:block;height:100%;background:linear-gradient(90deg,#1d4ed8,#60a5fa)}
.btns{display:flex;gap:8px;margin:12px 0}
button.primary{background:linear-gradient(135deg,#1d4ed8,#0f3b75);color:#fff;border:0;border-radius:10px;padding:9px 14px;font-weight:800;cursor:pointer}
button.ghost{background:#f8fafc;border:1px solid var(--line);border-radius:10px;padding:9px 14px;font-weight:800;cursor:pointer}
.hidden{display:none}
@media(max-width:820px){.form-grid-2{grid-template-columns:1fr}.kpis{grid-template-columns:repeat(2,1fr)}}
```

- [ ] **Step 3: render.js 작성 (순수 렌더 헬퍼)**

`web/render.js`:

```js
// 순수 헬퍼 — DOM 문자열만 생성. window.Render 네임스페이스.
window.Render = (function(){
  function esc(s){return String(s==null?'':s).replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));}
  function ratePill(rate, applied){
    if(!applied) return '<span class="pill muted">-</span>';
    const pct=Math.round(rate*100);
    const cls = pct>=60?'good':pct>=30?'ok':pct>0?'warn':'bad';
    return `<span class="pill ${cls}">${pct}%</span>`;
  }
  function bar(rate){
    const pct=Math.round((rate||0)*100);
    return `<div class="bar"><i style="width:${pct}%"></i></div>`;
  }
  function recordTitle(r){
    const unit = r.unit?` · ${esc(r.unit)}`:'';
    return `${esc(r.university)} · ${esc(r.type)} ${esc(r.name)}${unit}`;
  }
  return {esc, ratePill, bar, recordTitle};
})();
```

- [ ] **Step 4: app.js 최소 골격(탭+검색 결과 렌더) 작성**

`web/app.js`:

```js
(function(){
  const KEYS = [
    {key:'jeonhyeong', label:'전형'},
    {key:'jeonhyeong_inmun', label:'전형·인문'},
    {key:'jeonhyeong_jayeon', label:'전형·자연'},
    {key:'hakgwa', label:'학과'},
  ];
  const state = {key:'jeonhyeong', loaded:{}};

  function loadData(key){
    return new Promise((resolve)=>{
      if(window.__APPDATA__ && window.__APPDATA__[key]) return resolve();
      const s=document.createElement('script');
      s.src=`data/${key}.js`;
      s.onload=()=>resolve();
      s.onerror=()=>resolve(); // 데이터 없음도 진행
      document.head.appendChild(s);
    });
  }

  function indexFor(key){ return (window.__APPINDEX__||[]).filter(x=>x.key===key); }

  function renderTabs(){
    const el=document.getElementById('sheetTabs');
    el.innerHTML=KEYS.map(k=>
      `<button class="tab ${k.key===state.key?'active':''}" data-key="${k.key}">${k.label}</button>`
    ).join('');
    el.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{state.key=b.dataset.key;renderTabs();fillRegions();runSearch();});
  }

  function fillRegions(){
    const regions=[...new Set(indexFor(state.key).map(x=>x.region).filter(Boolean))].sort();
    document.getElementById('regionFilter').innerHTML =
      '<option value="">전체</option>'+regions.map(r=>`<option>${r}</option>`).join('');
  }

  function runSearch(){
    const q=document.getElementById('q').value.trim();
    const region=document.getElementById('regionFilter').value;
    const terms=q.split(/\s+/).filter(Boolean);
    let rows=indexFor(state.key);
    if(region) rows=rows.filter(x=>x.region===region);
    if(terms.length) rows=rows.filter(x=>{
      const hay=`${x.university} ${x.type} ${x.name} ${x.unit||''}`;
      return terms.every(t=>hay.includes(t));
    });
    rows=rows.slice(0,200);
    document.getElementById('resultBody').innerHTML = rows.map(x=>
      `<tr class="clickable" data-id="${Render.esc(x.id)}">
        <td>${Render.esc(x.region)}</td>
        <td><b>${Render.esc(x.university)}</b></td>
        <td>${Render.esc(x.type)} ${Render.esc(x.name)}${x.unit?' · '+Render.esc(x.unit):''}</td>
      </tr>`).join('') || '<tr><td>검색 결과 없음</td></tr>';
    document.querySelectorAll('#resultBody tr.clickable').forEach(tr=>
      tr.onclick=()=>window.App.openDetail(tr.dataset.id));
  }

  window.App = {
    openDetail(id){ console.log('openDetail', id); }, // Task 6에서 구현
    state, loadData, indexFor,
  };

  async function init(){
    renderTabs(); fillRegions();
    await loadData(state.key); runSearch();
    document.getElementById('q').addEventListener('input', runSearch);
    document.getElementById('regionFilter').addEventListener('change', runSearch);
    document.getElementById('reportBadge').textContent =
      `${indexFor(state.key).length}개 전형`;
  }
  // 탭 전환 시 데이터 보장
  const origRun = runSearch;
  document.addEventListener('DOMContentLoaded', init);
})();
```

- [ ] **Step 5: 브라우저 실측 검증**

Run: 브라우저에서 `web/index.html` 더블클릭(또는 `start web/index.html`).
확인:
- 헤더·탭 4개 표시, 배지에 개수 표시
- 검색창에 "서울대" 입력 시 결과 표가 좁혀짐
- 지역 필터 동작
- 탭 전환 시 해당 시트 데이터 로드(콘솔 에러 없음)
- 행 클릭 시 콘솔에 `openDetail <id>` 출력

> 주의: 탭 전환 시 데이터가 아직 안 불러와졌으면 비어 보일 수 있음 → Step 6에서 보강.

- [ ] **Step 6: 탭 전환 시 비동기 로드 보강**

`app.js`의 `renderTabs` 내 탭 클릭 핸들러를 교체:

```js
    el.querySelectorAll('.tab').forEach(b=>b.onclick=async()=>{
      state.key=b.dataset.key; renderTabs(); fillRegions();
      await loadData(state.key); runSearch();
      document.getElementById('reportBadge').textContent=`${indexFor(state.key).length}개 전형`;
    });
```

- [ ] **Step 7: 재검증 + 커밋**

브라우저에서 학과 탭까지 전환되며 결과가 뜨는지 확인 후:
```bash
git add web/index.html web/styles.css web/app.js web/render.js
git commit -m "feat: 프런트 뼈대 + 검색 화면 + 디자인 토큰"
```

---

## Task 6: 순위표 상세 화면 (요약 카드 + 같이 지원한 전형)

**Files:**
- Modify: `web/app.js`, `web/render.js`

- [ ] **Step 1: render.js에 상세 렌더 추가**

`web/render.js`의 return 직전에 함수 추가하고 export에 포함:

```js
  function summaryCard(r){
    const g=r.grades||{}; const c=r.count||{};
    const kpi=(label,val)=>`<div class="kpi"><span>${label}</span><strong>${val==null?'-':val}</strong></div>`;
    return `<div class="card">
      <h2>${recordTitle(r)}</h2>
      <div class="kpis">
        ${kpi('사례수', `${c.applied} <small style="font-size:13px;color:#64748b">[${c.accepted}]</small>`)}
        ${kpi('합격률', `${Math.round((r.rate||0)*100)}%`)}
        ${kpi('내신 최고', g.best)}
        ${kpi('내신 평균', g.avg)}
        ${kpi('내신 최저', g.worst)}
      </div>
    </div>`;
  }
  function prefTable(r){
    const rows=(r.preferences||[]).map(p=>`
      <tr class="clickable" data-rank="${p.rank}">
        <td class="rank" style="font-weight:800;color:#1d4ed8">${p.rank}</td>
        <td><b>${esc(p.university)}</b>${p.label?' · '+esc(p.label):''}</td>
        <td style="text-align:right">${p.applied}</td>
        <td style="text-align:right">${p.accepted}</td>
        <td style="width:160px">${bar(p.rate)} <small>${Math.round((p.rate||0)*100)}%</small></td>
        <td>${p.matchedId?'<span class="pill ok">상세</span>':'<span class="pill muted">역방향 없음</span>'}</td>
      </tr>`).join('');
    return `<div class="card">
      <h2>같이 지원한 전형 (지망순) — 행 더블클릭 시 상세</h2>
      <div class="scroll">
        <table><thead><tr>
          <th>지망</th><th>대학 · 전형</th><th style="text-align:right">지원</th>
          <th style="text-align:right">합격</th><th>합격률</th><th></th>
        </tr></thead><tbody>${rows||'<tr><td>데이터 없음</td></tr>'}</tbody></table>
      </div>
    </div>`;
  }
```
export 객체에 `summaryCard, prefTable` 추가.

- [ ] **Step 2: app.js openDetail 구현**

`app.js`의 `openDetail`를 교체:

```js
    openDetail(id){
      const rec=(window.__APPDATA__[state.key]||[]).find(r=>r.id===id);
      if(!rec) return;
      state.current=rec;
      const el=document.getElementById('detail');
      el.innerHTML = Render.summaryCard(rec) + Render.prefTable(rec);
      el.scrollIntoView({behavior:'smooth'});
      // 더블클릭 → 드릴인 (Task 7)
      el.querySelectorAll('tbody tr.clickable').forEach(tr=>{
        tr.ondblclick=()=>window.App.openDrill(rec, parseInt(tr.dataset.rank,10));
      });
    },
    openDrill(rec, rank){ console.log('drill', rec.id, rank); }, // Task 7
```

- [ ] **Step 3: 브라우저 실측 검증**

브라우저 새로고침 → 결과 행 클릭 시:
- 요약 카드(사례수 N[M], 합격률, 내신 KPI) 표시
- 같이 지원한 전형 순위표 + 합격률 막대 표시
- 매칭된 행엔 "상세" 배지, 안된 행엔 "역방향 없음" 배지
- 행 더블클릭 시 콘솔에 `drill <id> <rank>` 출력

- [ ] **Step 4: 커밋**

```bash
git add web/app.js web/render.js
git commit -m "feat: 순위표 상세 화면(요약 카드 + 같이 지원한 전형)"
```

---

## Task 7: 드릴인 — SVG 흐름도 + 양방향 비교

**Files:**
- Modify: `web/render.js`, `web/app.js`

- [ ] **Step 1: render.js에 SVG 허브-스포크 + 양방향 추가**

```js
  function hubSpoke(rec){
    const prefs=(rec.preferences||[]).slice(0,8);
    const W=720,H=360,cx=W/2,cy=H/2;
    const maxApplied=Math.max(1,...prefs.map(p=>p.applied));
    let edges='',nodes='';
    prefs.forEach((p,i)=>{
      const ang=(-Math.PI/2)+(i*2*Math.PI/prefs.length);
      const x=cx+Math.cos(ang)*240, y=cy+Math.sin(ang)*140;
      const w=1+ (p.applied/maxApplied)*9;
      edges+=`<line x1="${cx}" y1="${cy}" x2="${x}" y2="${y}" stroke="#93c5fd" stroke-width="${w.toFixed(1)}" stroke-opacity="0.8"/>`;
      nodes+=`<g><circle cx="${x}" cy="${y}" r="30" fill="#eff6ff" stroke="#1d4ed8"/>
        <text x="${x}" y="${y-2}" text-anchor="middle" font-size="11" font-weight="700" fill="#0f3b75">${esc(p.university)}</text>
        <text x="${x}" y="${y+13}" text-anchor="middle" font-size="10" fill="#475569">${p.applied}명</text></g>`;
    });
    return `<div class="card"><h2>교차지원 흐름도</h2>
      <svg viewBox="0 0 ${W} ${H}" width="100%" style="max-height:380px">
        ${edges}
        <circle cx="${cx}" cy="${cy}" r="42" fill="#1d4ed8"/>
        <text x="${cx}" y="${cy-3}" text-anchor="middle" font-size="12" font-weight="800" fill="#fff">${esc(rec.university)}</text>
        <text x="${cx}" y="${cy+14}" text-anchor="middle" font-size="10" fill="#dbeafe">${esc(rec.name).slice(0,8)}</text>
        ${nodes}
      </svg></div>`;
  }
  function bidirectional(rec, pref, target){
    const aToB = rec.count.applied? pref.applied/rec.count.applied : 0;
    let revText='<span class="pill muted">역방향 데이터 없음</span>';
    if(target){
      const back=(target.preferences||[]).find(p=>p.matchedId===rec.id);
      const bToA = (back && target.count.applied)? back.applied/target.count.applied : 0;
      revText = back
        ? `${esc(target.university)} 지원자 중 <b>${Math.round(bToA*100)}%</b>가 ${esc(rec.university)}에도 지원`
        : revText;
    }
    return `<div class="card"><h2>양방향 비교</h2>
      <p>${esc(rec.university)} ${esc(rec.name)} 지원자 중 <b>${Math.round(aToB*100)}%</b>가
         ${esc(pref.university)} ${esc(pref.label)}에도 지원</p>
      ${bar(aToB)}
      <p style="margin-top:14px">${revText}</p>
    </div>`;
  }
```
export에 `hubSpoke, bidirectional` 추가.

- [ ] **Step 2: app.js openDrill 구현**

```js
    openDrill(rec, rank){
      const pref=(rec.preferences||[]).find(p=>p.rank===rank);
      if(!pref) return;
      const target = pref.matchedId
        ? (window.__APPDATA__[state.key]||[]).find(r=>r.id===pref.matchedId)
        : null;
      const el=document.getElementById('detail');
      const drill=document.createElement('div');
      drill.innerHTML =
        `<div class="card" style="background:#f8fbff">
           <div class="btns"><button class="ghost" id="closeDrill">← 순위표로</button></div>
           <h2 style="margin:0">드릴인: ${Render.esc(rec.university)} ${Render.esc(rec.name)} → ${Render.esc(pref.university)} ${Render.esc(pref.label)}</h2>
         </div>` +
        Render.hubSpoke(rec) +
        Render.bidirectional(rec, pref, target);
      el.appendChild(drill);
      drill.scrollIntoView({behavior:'smooth'});
      drill.querySelector('#closeDrill').onclick=()=>drill.remove();
    },
```

- [ ] **Step 3: 브라우저 실측 검증**

- 상세 화면에서 순위표 행 **더블클릭** → 드릴인 카드 추가
- 교차지원 흐름도(중심 노드 + 스포크, 선 굵기가 지원수에 비례) 표시
- 양방향 비교: A→B 비율 + (매칭 시) B→A 비율, 미매칭 시 "역방향 데이터 없음"
- "← 순위표로" 클릭 시 드릴인 닫힘

- [ ] **Step 4: 커밋**

```bash
git add web/app.js web/render.js
git commit -m "feat: 드릴인 SVG 흐름도 + 양방향 비교"
```

---

## Task 8: 최종 통합 검증 & 사용 안내

**Files:**
- Create: `README.md`

- [ ] **Step 1: 전체 파이썬 테스트 재실행**

Run: `python -m pytest build/tests/ -v`
Expected: 전부 PASS

- [ ] **Step 2: 클린 빌드 후 산출물 확인**

```bash
rm -rf web/data && python build/build.py
ls web/data
```
Expected: `index.js jeonhyeong.js jeonhyeong_inmun.js jeonhyeong_jayeon.js hakgwa.js report.json`

- [ ] **Step 3: 브라우저 전 기능 점검 체크리스트**

`web/index.html` 더블클릭 후:
- [ ] 4개 탭 전환 + 각 시트 검색 동작
- [ ] 지역 필터 + 키워드 검색 동시 동작
- [ ] 행 클릭 → 요약 카드 + 순위표
- [ ] 순위표 합격률 막대 + 배지 정상
- [ ] 행 더블클릭 → 흐름도 + 양방향 비교
- [ ] 매칭 실패 행은 "역방향 없음"으로 표시(에러 아님)
- [ ] 모바일 폭(개발자도구)에서 레이아웃 깨지지 않음

- [ ] **Step 4: README 작성**

`README.md`:

```markdown
# 2026 지원경향 분석 웹앱

작년 입시 교차지원·합격 경향 조회 도구.

## 사용 (학생·선생님)
`web/` 폴더의 `index.html` 더블클릭. 설치·인터넷 불필요.
공유: `web/` 폴더를 통째로 전달(USB/압축/카톡) 또는 GitHub Pages 업로드.

## 자료 갱신 (관리자)
1. 새 엑셀로 `build/build.py`의 `XLSX` 경로 수정
2. `python build/build.py` 실행 → `web/data/*.js` 갱신
3. 빌드 끝에 시트별 행수·매칭률 출력. 상세는 `web/data/report.json`.

## 한계
지망 칸 표기와 원본 행 표기 차이로 역방향 매칭은 best-effort(100% 아님).
미매칭은 "역방향 데이터 없음"으로 표시되며 순위표·카드는 항상 정확.

## 개발 테스트
`python -m pytest build/tests/ -v`
```

- [ ] **Step 5: 최종 커밋**

```bash
git add README.md web/data/report.json
git commit -m "docs: README + 최종 통합 검증"
```

---

## Self-Review (작성자 점검 결과)

- **스펙 커버리지**: 검색(Task5)·순위표 요약카드(Task6)·합격률 막대(Task6)·드릴인 흐름도+양방향(Task7)·역방향 매칭+배지(Task2,4,7)·정적 산출물 file:// 동작(Task4,5)·디자인 토큰(Task5)·파서/매칭 테스트(Task1-3)·빌드 리포트(Task4)·README 한계 명시(Task8) 모두 대응됨.
- **플레이스홀더**: 없음(모든 코드·명령·기대출력 명시).
- **타입 일관성**: `parse_pref_cell`/`parse_sheet` 반환 키, `record`/`preference` 필드명(`matchedId`,`rate`,`count.applied`…)이 빌드(Task4)·프런트(Task6,7) 전반에서 일치.
- **스펙과의 차이(의도적)**: 네트워크 라이브러리 → 의존성 없는 순수 SVG, 데이터 전달 → `*.js` 전역주입(둘 다 file:// 더블클릭 보장을 위한 개선).
